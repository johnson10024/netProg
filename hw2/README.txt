Usage:

	1. "$make"
	2. "$./server"
	3. "$./client" in multiple terminals
	4.  In every client:
		4.1 "login"
		4.2 "playwith <rival_user_number>", rival_user_number is a logged in user
		4.3 "go <pos1> <pos2>" in each client(host and rival), 0 <= pos1, pos2 <= 3
